There are <%= pluralize(@person_count1, 'fish') %> signed up.
There are <%= pluralize(@person_count2, 'fish') %> signed up.

<%= truncate(@story, :length => 20) %>

<ul>
  <% @numbers.each do |num| %>
    <li style='background-color:<%= cycle('red', 'green', 'orange', 'purple') %>;'><%= num %></li>
  <% end %>
</ul>
